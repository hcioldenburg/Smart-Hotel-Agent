# Smart Home Diagnostic API — backend dependencies
# Install:  pip install -r requirements.txt
# Run (from the PARENT folder that contains smart_home_agent_backend/):
#   python -m smart_home_agent_backend.api

# Web / API
fastapi==0.138.1
uvicorn[standard]==0.49.0
python-dotenv==1.2.2
pydantic==2.13.4

# Agent graph + LLM
langchain==1.3.11
langgraph==1.2.6
langchain-openai==1.3.3   # provider for init_chat_model("gpt-5.4"); needs OPENAI_API_KEY

# Config / data
PyYAML==6.0.3
